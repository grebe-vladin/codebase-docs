# Framework notes

One file per framework, loaded only for the repos the module touches (detection table in `DISCOVERY.md`). Each answers the same six questions: where routes live, how a handler is found, where the data model is, where background work is, where config is read, where tests are. Add a framework by copying the shortest file and answering the six questions for it.
