---
type: llm
weight: 1
target: { source: file, path: docs/orders/orders.md }
---
The document's first section ("Summary") must, in at most ~150 words, say what the orders module does, why it exists, and how it works in one sentence (the mechanism). Every route in the API table must correspond to a route defined in the fixture code (orders controller). No placeholder text such as TODO or lorem may appear. PASS only if all three hold.
